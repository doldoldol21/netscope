<!-- Thanks for contributing! Keep PRs focused on one change. -->

## What & why

Closes #

<!-- Outside contributions need a linked issue (a CI check enforces it) — open
     one first so the problem can be discussed apart from the code that fixes
     it. Maintainers can add the `no-issue` label for housekeeping changes. -->

## How tested

<!-- Commands run / manual steps. e.g. make test, make demo, real capture -->

## Checklist

- [ ] `make fmt` (no diff), `make vet`, `make test` all pass
- [ ] Added/updated tests for behavior changes
- [ ] Updated README / flags if user-facing behavior changed
- [ ] One focused change; commit messages follow the convention
